module "" {
  source              = ".//the-name"
  bucket-name         = var.ASSETS_BUCKET_CMS_NAME
  AWS_DEFAULT_REGION  = var.AWS_DEFAULT_REGION
}

module "" {
  source              = ".//the-name"
  bucket-name         = var.ASSETS_BUCKET_CMS_NAME
  AWS_DEFAULT_REGION  = var.AWS_DEFAULT_REGION
}